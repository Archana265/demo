spring:  
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Archana265/cloud-config
          basedir: D:\git_samples\cloud-config 
            
            
server:  
  port: 8888
security:
  basic:
    enabled: false
    